# .env — configurações locais (NUNCA subir ao Git!)

# Ligação à base de dados (Timescale/Postgres no Docker)
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/mep

# API Key para autenticação
APIKEY_ENABLED=1
APIKEYS=secret123

# Rate limiting
RATE_LIMIT_ENABLED=1
RATE_LIMIT_RPS=5
RATE_LIMIT_BURST=10